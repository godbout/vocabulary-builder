version: '2'

options:
  compose-file-path: 'docker-compose.yml'
  compose-dev-file-path: 'docker-compose-dev.yml'
  verbose: true

syncs:
  vb_sync:
    notify_terminal: true
    src: '~/Dropbox/Development/vocabulary-builder/source'
    sync_strategy: 'native_osx'
    sync_excludes: ['.git']
    sync_userid: '1000'
